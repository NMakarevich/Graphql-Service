type Band {
    id: ID!
    name: String
    origin: String
    members: [Member]
    website: String
    genres: String
}

type Member {
    artist: Artist
    instrument: String
    years: [Int]
}

extend type Query {
    bands: [Band]
    band(id: ID!): Band
}